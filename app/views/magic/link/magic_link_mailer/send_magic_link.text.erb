Hey there! Here is your sign in link (you can only use it once):

<% if @url.present? %>
  <%= url_to @url, **parms.merge(email: @email, sign_in_token: @token) %>
<% else %>
  <%= main_app.root_url(email: @email, sign_in_token: @token) %>
<% end %>

Thanks!
